apiVersion: v1
kind: Service
metadata:
    name: "dev-050a"
    labels:
        app: "dev-050a"
spec:
    type: LoadBalancer
    ports:
    - port: 80
      targetPort: 80
      protocol: TCP
      name: http
    selector:
        app: "dev-050a"